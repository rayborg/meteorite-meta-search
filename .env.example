# Copy this file to .env for local scraper runs that need optional API credentials.
# Keep real values only in .env, shell environment variables, or GitHub Actions secrets.

EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
